<?xml version='1.0' encoding='UTF-8' ?>
<effect id="com.alightcreative.effects.tilerotate2" name="@am:string/effect_tilerotate_name" desc="@am:string/effect_tilerotate_desc" category="distort" tags="distort,geometric,raster" thumb="thumb/tilerotation.webp">

    <params>
        <texture id="inputImg" srcType="content" />
        <spinner id="rotation" default="45" min="0.0" max="3600.0" label="@am:string/effect_param_rotation" type="angle" ticks="0,45,90,135,180,225,270,315,360" step="0.5" />
        <!--<spinner id="offset" default="0.0" min="-2.0" max="2.0" step="0.01" label="@am:string/effect_param_offset" />-->
        <point id="position" label="@am:string/effect_param_offset" />
        <spinner id="amount" default="10.0" min="0.5" max="200.0" label="@am:string/effect_param_count" step="0.1" ticks="10" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <spinner id="angle" default="0.0" min="0.0" max="3600.0" label="@am:string/effect_param_angle" type="angle" ticks="0,45,90,135,180,225,270,315,360" step="0.5" />
        <section text="@am:string/effect_section_random" />
        <spinner id="strength" default="0.0" min="0.0" max="10.0" step="0.01" label="@am:string/effect_param_strength" />
        <spinner id="seed" default="0.0" min="0.0" max="5.0" step="0.01" label="@am:string/effect_param_seed" />
        <switch id="screenSpace" default="false" label="@am:string/effect_param_screenspace" />
    </params>

    <shader type="fragment" precision="high"><![CDATA[

        #define l2s(v) ((vec4(v,0.,1.)*acLayerToScreen).xy)

        float rand(vec2 co)
        {
            float a = 719.310;
            float b = 178.233;
            float c = 33758.5453;
            float dt= dot(co.xy ,vec2(a,b));
            float sn= mod(dt,3.14);
            return fract(sin(sn) * c);
        }

        void main() {
            float offset = 0.5;
            vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
            vec2 relPos = (position/1200.0);
            float layerSizeMag = length(acLayerSize);
            vec2 st;
            if(screenSpace) {
                st = acScreenNorm;
                st.y = 1.0  - st.y;
                st -= 0.5;
                st.x *= acScreenSize.x/acScreenSize.y;
                st -= relPos;
            } else {
                st = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/layerSizeMag-relPos;
            }
            
            float a = angle * 0.0174533;
            float ra = rotation * 0.0174533;
            st *= mat2(
                cos(a), -sin(a),
                sin(a), cos(a)
            );
            float px = floor(st.x*amount/stretch.x);
            float py = floor(st.y*amount/stretch.y);
            float rmag = rand(vec2(px,py+3937.7249*seed));
            ra += (rmag-0.5)*2.0*6.28318*strength;
            vec2 pcenter = vec2((px+offset)/(amount/stretch.x),(py+offset)/(amount/stretch.y));
            pcenter *= mat2(
                cos(-a), -sin(-a),
                sin(-a), cos(-a)
            );
            if(screenSpace) {
                pcenter += relPos;
                pcenter *= (acScreenSize.y/acScreenSize.x);
                pcenter += 0.5;
                pcenter.y = 1.0-pcenter.y;
            }
            else {
                pcenter += relPos;
                pcenter *= layerSizeMag;
                pcenter += acLayerCenter;
                pcenter /= acScreenSize;
                pcenter.y = 1.0-pcenter.y;
            }
            
            vec2 sampCoord = acScreenNorm;
            sampCoord -= pcenter;
            sampCoord *= mat2(
                cos(ra), -sin(ra),
                sin(ra), cos(ra)
            );
            sampCoord += pcenter ;
            //float dx = rand(vec2(px,py+3937.7249*seed));
            //float dy = rand(vec2(px+328.7319+432.7249*seed,py-971.382));
            //vec2 dv = (vec2(dx,dy) - 0.5)*2.0;
            //vec2 sampCoord = acScreenNorm+dv*strength;
            sampCoord = abs(mod(sampCoord-1.0,2.0)-1.0);
            vec4 sampColor = texture2DCv(inputImg.texture,sampCoord);
            //sampColor.r = length(acScreenNorm-pcenter);
            //sampColor.g = length(acScreenNorm-pcenter);
            gl_FragColor = sampColor * texColor.a;
        }

    ]]></shader>

</effect>

