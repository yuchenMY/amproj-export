<?xml version='1.0' encoding='UTF-8' ?>
<effect id="com.alightcreative.effects.lumakey2" compat="acScreenNorm-y" name="@am:string/effect_lumakey_name" desc="@am:string/effect_lumakey_desc" category="matte" tags="matte,mask,key,compositing,membersOnly,glitch,raster" thumb="thumb/luma-key.png" deprecated="true">

    <!--<strings>-->
        <!--<locale lang="en">-->
            <!--<string name="name">Luma Key</string>-->
            <!--<string name="desc">Makes pixels in the layer transparent based on how light or dark they are.</string>-->
            <!--<string name="threshold">Threshold</string>-->
            <!--<string name="feather">Feather</string>-->
            <!--<string name="invert">Invert</string>-->
        <!--</locale>-->
    <!--</strings>-->

    <params>
        <texture id="inputImg" srcType="content" />
        <spinner id="lowThreshold" default="0" min="0" max="1" step="0.001" label="@am:string/effect_param_low"/>
        <spinner id="highThreshold" default="1" min="0" max="1" step="0.001" label="@am:string/effect_param_high" />
        <slider id="feather" default="0.05" min="0" max="1" step="0.001" label="@am:string/effect_param_feather" />
        <switch id="invert" default="false" label="@am:string/effect_param_invert" />
        <switch id="weighted" default="true" label="@am:string/effect_param_weighted" />
    </params>

    <shader type="fragment"><![CDATA[

        const vec3 luminanceWeighting = vec3(0.2126, 0.7152, 0.0722);
        const vec3 uniformWeighting = vec3(0.3333, 0.3334, 0.3333);

        void main() {
            vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
            float luma = weighted ? length(texColor.rgb * luminanceWeighting) : length(texColor.rgb * uniformWeighting);
            //float alpha = smoothstep( threshold-feather/2., threshold+feather/2., length(texColor.rgb) );
            float lowThresholdA = clamp(lowThreshold-feather/2.,0.,1.);
            float lowThresholdB = clamp(lowThresholdA+feather,0.,1.);
            float highThresholdA = clamp(highThreshold-feather/2.,0.,1.);
            float highThresholdB = clamp(highThresholdA+feather,0.,1.);
            float lowBlend = smoothstep( lowThresholdA, lowThresholdB, luma );
            float highBlend = 1. - smoothstep( highThresholdA, highThresholdB, luma );
            float alpha = min(lowBlend,highBlend);
            if(invert) alpha = 1.-alpha;
            gl_FragColor = texColor*alpha;
        }

    ]]></shader>

</effect>
