<?xml version='1.0' encoding='UTF-8' ?>
<effect
    id="com.alightcreative.effects.transform"
    name="@am:string/effect_raster_transform_name"
    desc="@am:string/effect_raster_transform_desc"
    category="transform"
    tags="transform,raster"
    thumb="thumb/stretch.webp"
    experimental="false">
    
    <params>
        <texture id="inputImg" srcType="content" />
        <spinner id="scale" default="1" min="0.01" max="50.0" step="0.01" label="@am:string/effect_param_scale" ticks="0" />
        <spinner id="angle" default="0.0" min="0.0" max="3600.0" label="@am:string/effect_param_angle" type="angle" step="0.5" ticks="0,45,90,135,180,225,270,315,360" snap="45,90,135" />
        <point id="offset" label="@am:string/effect_param_offset" />
        <switch id="maskToLayer" label="@am:string/effect_param_masktolayer" default="false" />
        <spinner id="alpha" default="1.0" min="0" max="1" step="0.01" label="@am:string/effect_param_alpha" type="percent" />
        <spinner id="fill" default="0.0" min="0.0" max="1.0" label="@am:string/effect_param_fill" type="percent" step="0.01" ticks="0.5" />
        <selector id="sample" style="radio" default="1" label="@am:string/effect_param_sampling">
            <choice label="@am:string/effect_param_nearest" value="0"/>
            <choice label="@am:string/effect_param_linear" value="1"/>
        </selector>
    </params>

    vec4 composite( vec4 a, vec4 b) {
        return a * (1. - b.a) + b;
    }

    <shader type="fragment"><![CDATA[

        mat2 rot2(float a) {
            float c = cos(a);
            float s = sin(a);
            return mat2(c, -s, s, c);
        }
        
        vec2 l2s( vec2 v ) {
            return (vec4(v,0.,1.)*acLayerToScreen).xy;
        }

        vec4 composite( vec4 a, vec4 b) {
            return a * (1. - b.a) + b;
        }

        void main() {
        
            vec2 st = acLayerNorm;
            st -= offset/500.*vec2(1.,-1.);
            st -= 0.5;
            st.x *= acLayerSize.x/acLayerSize.y;
            st *= rot2( -angle * 0.0174533 );
            st /= scale;
            st.x /= acLayerSize.x/acLayerSize.y;
            st += 0.5;

            vec2 sampleCoord = l2s(st);
            if( sample == 0 ) {
                sampleCoord *= acScreenSize;
                sampleCoord = floor(sampleCoord) + 0.5;
                sampleCoord /= acScreenSize;
            }

            vec4 scaledTexColor = texture2DCv(inputImg.texture,sampleCoord);
            if( maskToLayer ) {
                vec4 baseColor = texture2DCv(inputImg.texture,acScreenNorm);
                gl_FragColor = composite(baseColor*fill, scaledTexColor*baseColor.a*alpha);
            } else if( fill > 0.0001 ) {
                vec4 baseColor = texture2DCv(inputImg.texture,acScreenNorm);
                gl_FragColor = composite(baseColor*fill, scaledTexColor*alpha);
            } else {
                gl_FragColor = scaledTexColor * alpha;
            }
            
        }

    ]]></shader>

</effect>
