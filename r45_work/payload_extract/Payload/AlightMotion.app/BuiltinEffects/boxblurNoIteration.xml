<?xml version='1.0' encoding='UTF-8' ?>
<effect id="com.alightcreative.effects.boxblur3" compat="acScreenNorm-y" name="@am:string/effect_boxblur_name" desc="@am:string/effect_boxblur_desc" category="blur" thumb="thumb/boxblur.webp" overdraw="true" maxOverdraw="0.1" tags="blur, raster">

    <!--<strings>-->
        <!--<locale lang="en">-->
            <!--<string name="name">Box Blur</string>-->
            <!--<string name="desc">Blur the layer by averaging the colors of nearby pixels.</string>-->
            <!--<string name="strength">Strength</string>-->
            <!--<string name="iterations">Iterations</string>-->
            <!--<string name="tip">More iterations give better quality, but slower preview performance. We recommend 2 iterations.</string>-->
        <!--</locale>-->
    <!--</strings>-->

    <params>
        <texture id="inputImg" srcType="content" />
        <texture id="hblur" srcType="buffer" />
        <spinner id="strength" default="0.15" min="0.0" max="1.0" label="@am:string/effect_param_strength" step="0.005" />
        <tip text="@am:string/effect_boxblur3_tip" />
    </params>
    
    <passes>
        <pass target="hblur" blend="overwrite" />
        <pass blend="overwrite" />
    </passes>

    <shader type="fragment"><![CDATA[

        void main() {
            if( acPass==0 ) {

                float angle = 0.0;
                float actualStrength = strength/4.0;

                vec2 texelSize = 1.0 / getTexSize(inputImg.size);
                float rad = angle * 0.0174533;
                vec2 velocity = vec2(cos(rad)*actualStrength,-sin(rad)*actualStrength);
                float speed = length(velocity / texelSize);
                int nSamples = int(clamp(speed, 1.01, 100.01));
                vec4 outColor = texture2DCv(inputImg.texture,acScreenNorm.st);
                for(int i=1; i<nSamples; i++ ) {
                    vec2 offset = velocity * (float(i) / float(nSamples - 1) - 0.5);
                    outColor += texture2DCv(inputImg.texture, acScreenNorm.st-offset);
                }
                gl_FragColor = outColor/float(nSamples);

            } else {

                float angle = 90.0;
                float actualStrength = strength/4.0;

                vec2 texelSize = 1.0 / getTexSize(inputImg.size);
                float rad = angle * 0.0174533;
                vec2 velocity = vec2(cos(rad)*actualStrength,-sin(rad)*actualStrength);
                float speed = length(velocity / texelSize);
                int nSamples = int(clamp(speed, 1.01, 100.01));
                vec4 outColor = texture2DCv(hblur.texture,acScreenNorm.st);
                for(int i=1; i<nSamples; i++ ) {
                    vec2 offset = velocity * (float(i) / float(nSamples - 1) - 0.5);
                    outColor += texture2DCv(hblur.texture, acScreenNorm.st-offset);
                }
                gl_FragColor = outColor/float(nSamples);

            }
            //int from = -max(1,int(strength));
            //int to = max(1,int(strength));
            //int step;
            //if( strength < 0.5 ) {
            //    step = 1
            //} else {
            //    step = max(1,int(log2(strength))-1);
            //}
            //vec4 outColor = vec4(0.0);
            //if( acPass==0 ) {
            //    for(int i=from; i<to; i += step ) {
            //        outColor += texture2DCv(inputImg.texture,vec2(acScreenNorm.s + float(i)*(1.0/1280.0),acScreenNorm.t));
            //    }
            //} else {
            //    for(int i=from; i<to; i += step ) {
            //        outColor += texture2DCv(hblur.texture,vec2(acScreenNorm.s,acScreenNorm.t + float(i)*(1.0/720.0)));
            //    }
            //}
            //gl_FragColor = outColor/(float(to)*2.0*float(step));
        }

    ]]></shader>
</effect>
