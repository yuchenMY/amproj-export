<?xml version='1.0' encoding='UTF-8' ?>
<effect id="com.alightcreative.effects.maskblur" compat="acScreenNorm-y" name="@am:string/effect_maskblur_name" desc="@am:string/effect_maskblur_desc" category="blur" tags="blur,raster" thumb="thumb/maskblur.webp" overdraw="false" maxOverdraw="0.001" >

    <!--<strings>-->
        <!--<locale lang="en">-->
            <!--<string name="name">Box Blur</string>-->
            <!--<string name="desc">Blur the layer by averaging the colors of nearby pixels.</string>-->
            <!--<string name="strength">Strength</string>-->
            <!--<string name="iterations">Iterations</string>-->
            <!--<string name="tip">More iterations give better quality, but slower preview performance. We recommend 2 iterations.</string>-->
        <!--</locale>-->
    <!--</strings>-->

    <params>
        <texture id="inputImg" srcType="content" />
        <texture id="comp" srcType="comp" />
        <texture id="hblur" srcType="buffer" />
        <spinner id="strength" default="0.35" min="0.0" max="1.0" label="@am:string/effect_param_strength" step="0.005" />
        <!--<slider id="iter" default="1" min="1" max="6" label="@am:string/effect_param_iterations" type="integer" step="1" />-->
        <selector id="channel" default="0" style="radio" label="@am:string/effect_param_channel" >
            <choice label="Lum" value="0" />
            <choice label="R" value="1" />
            <choice label="G" value="2" />
            <choice label="B" value="3" />
            <choice label="A" value="4" />
        </selector>
        <!--<tip text="@am:string/effect_boxblur_tip" />-->
    </params>

    <!--<iterations param="iter" />-->

    <passes>
        <pass target="hblur" blend="overwrite" />
        <pass />
    </passes>

    <shader type="fragment"><![CDATA[

        void main() {

            vec4 maskColor = texture2DCv(inputImg.texture,acScreenNorm);
            float mask;
            if( channel==0 ) {
                mask = maskColor.r*0.2126 + maskColor.g*0.7152 + maskColor.b*0.0722;
            } else if( channel == 1 ) {
                mask = maskColor.r;
            } else if( channel == 2 ) {
                mask = maskColor.g;
            } else if( channel == 3 ) {
                mask = maskColor.b;
            } else {
                mask = maskColor.a;
            }

            if( acPass==0 ) {

                float angle = 0.0;
                float actualStrength = (strength/4.0) * mask;

                vec2 texelSize = 1.0 / vec2(comp.size);
                float rad = angle * 0.0174533;
                vec2 velocity = vec2(cos(rad)*actualStrength,-sin(rad)*actualStrength);
                float speed = length(velocity / texelSize);
                int nSamples = int(clamp(speed, 1.01, 100.01));
                vec4 outColor = texture2DCv(comp.texture,acScreenNorm.st);
                for(int i=1; i<nSamples; i++ ) {
                    vec2 offset = velocity * (float(i) / float(nSamples - 1) - 0.5);
                    outColor += texture2DCv(comp.texture, acScreenNorm.st-offset);
                }
                gl_FragColor = outColor/float(nSamples);

            } else {

                float angle = 90.0;
                float actualStrength = (strength/4.0) * mask;

                vec2 texelSize = 1.0 / vec2(comp.size);
                float rad = angle * 0.0174533;
                vec2 velocity = vec2(cos(rad)*actualStrength,-sin(rad)*actualStrength);
                float speed = length(velocity / texelSize);
                int nSamples = int(clamp(speed, 1.01, 100.01));
                vec4 outColor = texture2DCv(hblur.texture,acScreenNorm.st);
                for(int i=1; i<nSamples; i++ ) {
                    vec2 offset = velocity * (float(i) / float(nSamples - 1) - 0.5);
                    outColor += texture2DCv(hblur.texture, acScreenNorm.st-offset);
                }
                gl_FragColor = outColor/float(nSamples) * maskColor.a;

            }
        }

    ]]></shader>

</effect>
