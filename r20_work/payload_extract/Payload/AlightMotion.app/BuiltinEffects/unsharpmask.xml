<?xml version='1.0' encoding='UTF-8' ?>
<effect
    id="com.alightcreative.effects.unsharpmask"
    name="@am:string/effect_unsharp_mask_name"
    desc="@am:string/effect_unsharp_mask_desc"
    category="blur"
    tags="blur,sharpen"
    thumb="thumb/glow.png">

    <params>
        <texture id="inputImg" srcType="content" />
        <texture id="hblur" srcType="buffer" />
        <spinner id="strength" default="0.02" min="0.0" max="2.0" step="0.01" label="@am:string/effect_param_strength" />
        <spinner id="amount" default="0.5" min="0.0" max="2.0" step="0.01" label="@am:string/effect_param_amount" type="percent" />
        <spinner id="threshold" default="0.5" min="-.5" max="1.0" step="0.001" label="@am:string/effect_param_threshold" type="percent" />
        <switch id="showMask" default="false" label="@am:string/effect_param_show_mask" />
    </params>
    
    <passes>
        <pass target="hblur" effect="com.alightcreative.effects.gaussianblur" />
        <pass />
    </passes>

    <shader type="fragment" precision="high"><![CDATA[

        const vec3 luminanceWeighting = vec3(0.2126, 0.7152, 0.0722);

        void main() {
            vec4 blur = texture2DCv(hblur.texture, acScreenNorm.st);
            vec4 base = texture2DCv(inputImg.texture, acScreenNorm.st);
            float blurY = dot(blur.rgb, luminanceWeighting);
            float baseY = dot(base.rgb, luminanceWeighting);
            vec4 adjColor = base * (1.+amount) - (blur*amount);
            float t = step(threshold,abs(blurY-baseY)*4.);
            t = smoothstep(threshold*0.75, (threshold+1.01)/2., abs(blurY-baseY)*4.);
            gl_FragColor = showMask ? vec4(t,0.,0.,1.)*base.a : mix(base,adjColor,t);
        }

    ]]></shader>

</effect>
