<?xml version='1.0' encoding='UTF-8' ?>
<effect id="com.alightcreative.effects.displacemap_scale" compat="acScreenNorm-y" name="displacement_scale" desc="displacement_scale" tags="distort" thumb="thumb/displacemap.png" experimental="true">

    <params>
        <texture id="srcImg" srcType="content" />
        <texture id="inputImg" srcType="buffer" />
        <texture id="comp" srcType="comp" />
        <slider id="refraction" default="0.67" min="0.1" max="2.0" label="refraction" />
        <spinner id="smoothing" default="50." min="1." max="200." step="0.1" label="@am:string/effect_param_smoothing" />
        <spinner id="height" default="0.5" min="-1." max="1." step="0.01" label="@am:string/effect_param_height" ticks="0., -0.5, 0.5" type="percent" />
        <spinner id="strength" default="0.02" min="0.0" max="2.0" step="0.01" label="blurring" />
    </params>
    <passes>
        <pass target="inputImg" effect="com.alightcreative.effects.gaussianblur" />
        <pass />
    </passes>

    <shader type="fragment" precision="high"><![CDATA[
        
        const vec3 luminanceWeighting = vec3(0.2126, 0.7152, 0.0722);

        void main() {
        
            vec4 baseImg = texture2DCv(inputImg.texture, acScreenNorm);
            if(baseImg.a <= 0.0) {
                gl_FragColor = texture2DCv(comp.texture, acScreenNorm);
                return;
            }
            
            
            vec2 st = acScreenNorm;
            vec2 fragSize;
            float curHeight = 0.0;
            float left = 0.0;
            float right = 0.0;
            float top = 0.0;
            float bottom = 0.0;
            
            fragSize = vec2(1.)/getTexSize(inputImg.size) * smoothing;
            curHeight = length(texture2DCv(inputImg.texture, st).xyz*luminanceWeighting);
            left = length(texture2DCv(inputImg.texture, vec2(st.x - fragSize.x, st.y) ).xyz*luminanceWeighting);
            right = length(texture2DCv(inputImg.texture, vec2(st.x + fragSize.x, st.y) ).xyz*luminanceWeighting);
            top = length(texture2DCv(inputImg.texture, vec2(st.x, st.y + fragSize.y) ).xyz*luminanceWeighting);
            bottom = length(texture2DCv(inputImg.texture, vec2(st.x, st.y - fragSize.y) ).xyz*luminanceWeighting);

            float adjHeight = -height / 100.0;
            curHeight *= adjHeight;
            left *= adjHeight;
            right *= adjHeight;
            top *= adjHeight;
            bottom *= adjHeight;

            vec3 va = normalize(vec3(fragSize.x*2.0,0.0,right-left));
            vec3 vb = normalize(vec3(0.0,fragSize.y*2.0,top-bottom));
            vec3 n = normalize(cross(va,vb));
            
            vec3 r = normalize(refract(vec3(0,0,1), n, refraction));
            
            gl_FragColor = texture2DCv(comp.texture, acScreenNorm + r.xy * r.z);
        }

    ]]></shader>

</effect>
