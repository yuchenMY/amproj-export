<?xml version='1.0' encoding='UTF-8' ?>
<effect id="com.alightcreative.effects.diff" compat="acScreenNorm-y" name="@string/name" desc="@string/desc" category="color" internal="true">

    <strings>
        <locale lang="en">
            <string name="name">Difference</string>
            <string name="desc">Diff.</string>
        </locale>
    </strings>

    <params>
        <texture id="inputImg" srcType="content" />
        <texture id="comp" srcType="comp" />
    </params>

    <shader type="fragment" precision="high"><![CDATA[

        void main() {
            vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
            vec4 compColor = texture2DCv(comp.texture,acScreenNorm);
            gl_FragColor = vec4(abs(compColor.rgb*texColor.a - texColor.rgb),texColor.a);
        }

    ]]></shader>

</effect>
