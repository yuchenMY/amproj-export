<?xml version='1.0' encoding='UTF-8' ?>
<effect id="com.alightcreative.effects.wipe" compat="acScreenNorm-y" name="@am:string/effect_wipe_name" desc="@am:string/effect_wipe_desc" category="matte" tags="mask,matte,raster" thumb="thumb/wipe3.webp" deprecated="true">

    <!--<strings>-->
        <!--<locale lang="en">-->
            <!--<string name="name">Directional Blur</string>-->
            <!--<string name="desc">Blurs the layer in a specific direction</string>-->
            <!--<string name="strength">Strength</string>-->
            <!--<string name="angle">Angle</string>-->
        <!--</locale>-->
    <!--</strings>-->

    <params>
        <texture id="inputImg" srcType="content" />
        <spinner id="start" default="0.0" min="-50.0" max="150.0" step="0.1" label="@am:string/effect_param_start" />
        <spinner id="end" default="100.0" min="-50.0" max="150.0" step="0.1" label="@am:string/effect_param_end" />
        <spinner id="angle" default="0.0" min="0.0" max="3600.0" label="@am:string/effect_param_angle" type="angle" step="0.5" ticks="0,45,90,135,180,225,270,315,360" snap="45,90,135" />
        <spinner id="feather" default="0.0" min="0.0" max="100.0" step="0.1" label="@am:string/effect_param_feather" />
    </params>

    <shader type="fragment"><![CDATA[

        //vec2 projectAndClamp(vec2 a, vec2 b) {
        //    return clamp(dot(a,b)/dot(b,b),0.0,1.0) * b;
        //}
        vec2 project(vec2 a, vec2 b) {
            return (dot(a,b)/dot(b,b)) * b;
        }

        void main() {
            float adjStart = start/100.0;
            float adjEnd = end/100.0;
            float adjFeather = feather/100.0;
            float rad = angle * 0.0174533;
            vec2 v = normalize(vec2(cos(rad),-sin(rad)));
            //vec2 v = (vec4(normalize(vec2(cos(rad),-sin(rad))),0.0,1.0) * acLayerToScreen).xy;
            //float p = length(projectAndClamp(acLayerNorm.st-vec2(0.5,0.5), v))/length(v)+0.5;
            float p = dot(acLayerNorm.st-vec2(0.5,0.5), v)+0.5;
            float p0 = smoothstep(adjStart-adjFeather,adjStart,p);
            float p1 = 1. - smoothstep(adjEnd,adjEnd+adjFeather,p);
            gl_FragColor = texture2DCv(inputImg.texture,acScreenNorm.st)*p0*p1;
        }

    ]]></shader>

</effect>
