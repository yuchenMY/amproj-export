<?xml version='1.0' encoding='UTF-8' ?>
<effect id="com.alightcreative.effects.stretchsegment" compat="acScreenNorm-y" name="@am:string/effect_stretchsegment_name" desc="@am:string/effect_stretchsegment_desc" category="distort" tags="distort,fun,raster,membersOnly" thumb="thumb/stretchseg3.webp">

    <params>
        <texture id="inputImg" srcType="content" />
        <spinner id="angle" default="0.0" min="0.0" max="3600.0" label="@am:string/effect_param_angle" type="angle" ticks="0,45,90,135,180,225,270,315,360" step="0.5" />
        <spinner id="stretch" default="10.0" min="0" max="900" label="@am:string/effect_param_stretch" step="1" ticks="0" />
        <spinner id="offset" default="0.0" min="-2000" max="2000" label="@am:string/effect_param_offset" step="1" ticks="0" />
        <spinner id="smooth" default="0.0" min="0" max="1" label="@am:string/effect_param_smoothing" step="0.01" ticks="0" />
    </params>

    <shader type="fragment" precision="high"><![CDATA[

        mat2 rot(float a) {
            float c = cos(a);
            float s = sin(a);
            return mat2(
                c, -s,
                s, c
            );
        }
        
        float sminCubic( float a, float b, float k )
        {
            float h = max( k-abs(a-b), 0.0 )/k;
            return min( a, b ) - h*h*h*k*(1.0/6.0);
        }


        void main() {
        
            vec2 v = vec2(1.0,0.0) * rot(angle * 0.0174533);
        
            vec2 st = acScreenNorm;
            st -= acLayerCenterNorm;
            
            float dist = dot(st,v) + (offset/1000.0);
            //float d = min(stretch/500.0,abs(dist));
            
            float adjStretch = stretch/500.0;
            
            float d = sminCubic(adjStretch,abs(dist),max(0.00001,smooth * adjStretch));
            
            vec4 color = texture2DCv(inputImg.texture,acScreenNorm + v * d * -sign(dist));
            
            if( acShowGuides ) {
                vec4 guideCenterColor = vec4(1.0, 0.0, 0.0, 1.0);
                vec4 guideStretchColor = vec4(1.0, 1.0, 0.0, 1.0);
                gl_FragColor = mix(mix(color,guideStretchColor, min(0.3,1.0-step(stretch/500.0,abs(dist)))  ),guideCenterColor,1.0-step(2.0/length(acScreenSize),abs(dist)));
            } else {
                gl_FragColor = color;
            }

        }

    ]]></shader>

</effect>

