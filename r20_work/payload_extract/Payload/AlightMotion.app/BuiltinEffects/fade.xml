<?xml version='1.0' encoding='UTF-8' ?>
<effect id="com.alightcreative.effects.fade" compat="acScreenNorm-y" name="@am:string/effect_fade_name" desc="@am:string/effect_fade_desc" category="opacity" tags="opacity,visibility,auto,propAdjust" thumb="thumb/fade.webp">

    <!--<strings>-->
        <!--<locale lang="en">-->
            <!--<string name="name">Fade In/Out</string>-->
            <!--<string name="desc">Fades a layer in at the beginning, and out again at the end. The duration of the fade-in and fade-out can be adjusted.</string>-->
            <!--<string name="in">In</string>-->
            <!--<string name="out">Out</string>-->
        <!--</locale>-->
    <!--</strings>-->

    <params>
        <spinner id="inTime" default="0.5" min="0.0" max="10.0" label="@am:string/effect_param_in" type="seconds" />
        <spinner id="outTime" default="0.5" min="0.0" max="10.0" label="@am:string/effect_param_out" type="seconds" />
    </params>

    <script lang="js"><![CDATA[

        // from https://gist.github.com/gre/1650294
        /*
         * Easing Functions - inspired from http://gizma.com/easing/
         * only considering the t value for the range [0, 1] => [0, 1]
         */
        let EasingFunctions = {
          // no easing, no acceleration
          linear: function (t) { return t },
          // accelerating from zero velocity
          easeInQuad: function (t) { return t*t },
          // decelerating to zero velocity
          easeOutQuad: function (t) { return t*(2-t) },
          // acceleration until halfway, then deceleration
          easeInOutQuad: function (t) { return t<.5 ? 2*t*t : -1+(4-2*t)*t },
          // accelerating from zero velocity
          easeInCubic: function (t) { return t*t*t },
          // decelerating to zero velocity
          easeOutCubic: function (t) { return (--t)*t*t+1 },
          // acceleration until halfway, then deceleration
          easeInOutCubic: function (t) { return t<.5 ? 4*t*t*t : (t-1)*(2*t-2)*(2*t-2)+1 },
          // accelerating from zero velocity
          easeInQuart: function (t) { return t*t*t*t },
          // decelerating to zero velocity
          easeOutQuart: function (t) { return 1-(--t)*t*t*t },
          // acceleration until halfway, then deceleration
          easeInOutQuart: function (t) { return t<.5 ? 8*t*t*t*t : 1-8*(--t)*t*t*t },
          // accelerating from zero velocity
          easeInQuint: function (t) { return t*t*t*t*t },
          // decelerating to zero velocity
          easeOutQuint: function (t) { return 1+(--t)*t*t*t*t },
          // acceleration until halfway, then deceleration
          easeInOutQuint: function (t) { return t<.5 ? 16*t*t*t*t*t : 1+16*(--t)*t*t*t*t }
        }

    ]]></script>

    <script lang="js"><![CDATA[

        function animate(env,el,param) {
            let t = env.time;
            let inTime = param.inTime / env.duration;
            let outTime = param.outTime / env.duration;
            if( t < inTime ) {
                el.alpha *= EasingFunctions.easeInOutQuad(t/inTime)
            }
            if ( t > 1.0-outTime ) {
                el.alpha *= EasingFunctions.easeInOutQuad((1.0-t)/outTime)
            }
            return el;
        }

    ]]></script>

</effect>
